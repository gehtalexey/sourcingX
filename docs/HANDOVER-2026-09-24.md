# Handover — 2026-09-24, SourcingX screening model work

For a new session picking this up cold. Full detail and the running plan live in
`docs/GOAL-screening-model-luna-jev.md` — read that file first, its own "Handover"
section (block 8) is kept current. This file is the short version.

## Shipped this session (all merged to `master`)

- **PR #133** — added a screening option using TypeSafe's Jev, built from
  agent-kalamata's real (unmerged) branch code. 7 real bugs found and fixed across 6
  rounds of independent review (Codex + a Fable second opinion before merge).
- **PR #134** — one setting (`screening_models.py` / `get_screen_model()`) now picks
  which AI model screens candidates, instead of it being typed into the code in several
  places. 2 Codex rounds, one real bug fixed (a deployed app's model setting was
  silently ignored).
- **PR #135** — `compare_screening_modes.py` had quietly drifted onto testing the wrong
  version of the screening prompt (a legacy path, not the one the live app actually
  uses) — fixed. 3 Codex rounds also caught and fixed two related problems: a "quick"
  mode option that had become a no-op duplicate of "detailed," and a cost-estimate table
  that would have understated real spend (it now refuses to show a dollar figure until
  real usage is measured, rather than showing a wrong one).

## Corrected mid-session (don't re-trip on this)

SourcingX's live screening model is **`gpt-5.6-luna`**, not `gpt-4.1-mini` — it switched
on 2026-09-17 (PR #131), a week before this work started. That switch already included
its own parameter fixes, retry-on-empty, and cost logging. This was discovered partway
through this session after operating on the wrong assumption for a while — always
re-check the live model in the code directly rather than trusting an older note.

## Still in flight — the next step

**The 200-profile bake-off.** Compare today's model, Luna, and Jev against what
agent-kalamata's own pipeline already decided on the same candidates. All the code this
needs is built and merged. It has **not been run** — it's real money (roughly 200
profiles × 2 models × ~2 calls each) and needs Alexey's explicit OK on the exact dollar
cost before anything runs. That's the one thing blocking it, nothing else.

## Deliberately not done

The "screening incomplete" idea (a clean way to flag when the AI's answer was
ambiguous, instead of the current design where an ambiguous answer never overrides an
existing GO) was intentionally left out — Alexey's call, 2026-09-23. Jev is listed in
the model table but not wired into live screening for the same underlying reason — it
has no "incomplete" outcome to land in yet.

## Standing constraint

Claude Code on this machine cannot push changes to `.github/workflows/*.yml` — a real
GitHub permission limit (missing `workflow` OAuth scope), not a bug, and not something
to ask Alexey to click through either. If a new test file needs to run in CI, fold it
into an existing test file the workflow already covers instead of adding a new one to
the list. Full detail: the `reference-github-workflow-scope-limit` memory entry.
